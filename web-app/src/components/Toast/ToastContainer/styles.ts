import styled from 'styled-components';

const Container = styled.div`
  display: grid;
  gap: 12px;
  position: fixed;
  bottom: 48px;
  left: 50%;
  transform: translateX(-50%);
`;

export { Container };
